# PostStudio 开发规则

本文件适用于整个仓库及后续开发任务。以下版本提交和发布规则由项目所有者明确要求，属于持续授权；执行时无需重复请求批准。

## 每个小版本完成后提交并推送

- 小版本指第三位版本号变化，例如 `0.3.0 → 0.3.1`。
- 每完成一个小版本，完成相应验证、同步版本号和文档后，必须创建 Git 提交并推送到远端，不能仅保留在本地。
- 推送后核对远端提交与本地一致；失败时保留本地提交，说明阻碍，恢复可用后继续推送，不得声称已上传。

## 完成新中版本时发布上一系列的最终小版本

- 中版本指第二位版本号变化，例如 `0.3 → 0.4`。
- 完成新中版本时，必须为紧邻的上一中版本系列中最后完成的小版本发布 GitHub Release，并附上打包好的软件。
- 例如完成 `0.4.0`，而上一系列最后完成的版本为 `0.3.5`，则发布 **`v0.3.5` Release**，不是 `v0.4.0`。
- 从历史提交和版本记录确认发布目标，不按当前工作区内容猜测，也不将当前版本的包改名为旧版本。
- 从目标版本的准确提交在独立目录构建，核对包内版本、启动与必要验证；保留依赖许可说明。使用指向该提交的 `vX.Y.Z` 标签，上传可供 macOS 用户解压使用的应用安装包（例如包含 `.app` 的 ZIP），并写明更新内容、支持平台与签名／公证状态。只有源码压缩包不算完成软件发布。
- 不覆盖已有标签或重复发布同版本；如历史源码、构建条件或上传权限缺失，明确记录阻碍，不发布不对应的安装包。

## 收尾

- 按上述规则执行后，更新版本记录和交接文档，并核对远端提交；发布 Release 时还需核对标签、附件和下载入口。
- 提交不包含私人照片项目、凭据、虚拟环境、缓存和本地构建目录；软件包通过 Release 附件发布。
- 此规则从接下来的开发开始适用；本次仅记录规则，不据此自动补发历史版本。
