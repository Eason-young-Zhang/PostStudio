## Git 整理 · 2026-09-20

建立 Git 管理，继承远端 main 初始提交与 MIT 许可证；重整 README，新增贡献指南、变更记录和第三方素材说明。旧构建包与生成 spec 归档到项目外；私人项目、素材、虚拟环境及缓存均不上传。整理后 62 项测试通过。

当前交付状态：0.4.1 已重新构建并安装到 ~/Applications/PostStudio.app，本地签名严格校验通过，旧包已备份在 artifacts/backups。62 项回归通过；新增总览截图和手册第 4.5 节。安装包启动成功，但本次 CUA 系统文件对话框按键异常，未完成安装包内项目重开验证；源码 Qt 测试已覆盖项目保存重开与界面保存撤销。自动化保持暂停。

## 2026-09-20 · 0.4.1 版本管理与外框比例

- 画布右键删除移入项目回收站，移除各画布摆放，保留依赖并支持恢复。
- 总览中央区域采用独立缩略图网格；切回画布保留自由布局。
- 显式重新编辑无下游引用的末端产物时可“保存修改”；保留身份与布局，支持会话撤销。回收站中的下游引用同样阻止覆盖；其他情形继续生成新稿。
- 边框新增指定外框比例、常用比例与自定义宽高比；扩展外框而不重采样照片，内遮禁用。
- 62 项自动回归通过，包含模型依赖、回滚、组合缓存、界面保存撤销、删除和比例像素验证。

# 最新状态：PostStudio 0.4.0 本地测试版已交付

2026-09-20：已完成本轮获批功能、56项自动回归、新效果原生性能测量、最新源码构建安装与严格本地签名、实际新格式项目重开和正常退出。19章使用手册及7张新版截图已更新，离线HTML已在浏览器核对；修复了延迟加载图片使章节锚点偏移的问题。细项和未测范围见 ACCEPTANCE_0_4.md。没有待继续编码的本轮阻塞项；节点真实鼠标拖动与外部绘画软件往返留作试用验证，不冒充已测。自动化 block-studio 已确认 PAUSED，检查间隔字段为20分钟；本轮不再自动启动开发。

下方为过程记录，旧的“剩余交付工作”已由本段及验收记录取代。

# PostStudio 执行接续

2026-09-18。用户已批准 NEXT_ACTION_PLAN.md，追加 PostStudio 更名及冷蓝 UI。**开发仍在进行，未打包交付 0.4.0，不得报告全部完成。**

## 配额与自动接续

heartbeat `block-studio`，ACTIVE，每 20 分钟检查；当前任务接续。最近查询五小时使用 32%，周使用 88%，可继续开发。每次接续重新查询，不凭旧记录猜测。全部交付前保留自动化，交付后暂停。

## 当前源码

- 更名 PostStudio、冷蓝主题、构建名和入口版本 0.4.0；保留旧 blockstudio 模块和 .blockproj 格式扩展名。旧应用未替换。
- 工作台画布／图像删除、回收站保存恢复和依赖保护；右键入口；预设直接执行与管理；撤销模型与取色取消。
- 配色：0.1% 参数、独立四边、同值／成对联动、自定色卡宽高及对齐、两种比例、外框同比例缩放、实际像素留边显示、照片／色卡阴影。留边字段为最小值，额外空间由内容位置分配。
- 分块：色卡候选图标、颜色按钮修复、Oklab 渐变和取样轮廓阴影；旧结果不联动。
- 胶片／EXIF：导入读取、旧项目补读、批量与单图资料覆盖；水印执行冻结字段快照。
- 水印：多文字／图片层、字段替换、缺失字段规则、本机字体、混合／描边／阴影、九宫格与拖动、照片／下边框参考。图片资源嵌入项目，复制打开后仍可执行。缺失字体在面板显示，修改其他参数不会偷偷替换字体。
- 边框：外扩／内遮、多层排序、四边联动、两种比例与外框缩放、填充／描边、圆角、渐变与阴影、四类模板；区域供后续题注定位。
- workflow.py：单条串行链、冻结每张字段、命名／多个导出／处理节点、失败继续下一张、取消报告、保留中间产物。导出降采样不影响后续工作尺寸。
- workflow_widgets.py：左侧预设动作库、中间顺序节点、右侧参数，拖入／排序／复制／停用／删除，显式从库更新当前节点，默认仅展示最终结果。流程内图内取色按钮禁用并解释应先在图像工具保存预设。
- effects.py 阴影改为条带前缀和，避免全图 float64 累加数组；中间阴影尺寸分配前限制 110 MP。最终效果性能仍需测量。
- QApplication 退出前先整体销毁窗口树，避免 PySide 在解释器退出时随机顺序销毁有信号引用的子控件；测试夹具同步采用完整父树清理。

## 已验证与证据

- 当前自动测试 **49 项通过，进程正常退出（exit 0）**。包含 10 张不同尺寸／颜色／资料的合成图、文字水印→命名→降采样 PNG 导出→继续反转、独立字段、资源随项目复制、取消与失败报告、缺字体保留、边框比例与原像素、条带阴影数值对照、布局联动。
- 修复前曾出现“44 项通过但进程退出 139”的 Qt 销毁崩溃，已定位到解释器退出的 QDoubleSpinBox 析构顺序；不可把旧 44 项通过当作无崩溃证据。最终安装包仍需关闭验收。
- CUA 实际原生源码应用：打开旧 palette-demo.blockproj；冷蓝工作台正常；独立取样页面；取色按钮取消与 Esc 返回取样图；图内取色后 ⌘Z 恢复旧底色及透明开关。颜色按钮未溢出。
- 原生水印页面发现窄侧栏横向溢出，已改为长表单换行、缩短宽度标签、图层动作两行；**此修改之后尚未重启复验**。
- 最新界面微调之后完整 49 项再次通过（exit 0）。随后修复原生关闭确认后的重入问题：选择“不保存”后延后至下一事件循环关闭；新增关闭回归。新功能测试模块 18 项通过，总计现有 50 项，最后新增后尚未重跑完整 50 项。
- 备份 artifacts/backups/source-before-poststudio.tar.gz；旧安装 Block Studio 0.3.1.app 不动。

## 继续顺序

1. 界面微调回归；原生重启检查窄侧栏、流程拖入排序、边框／水印中文和拖动撤销。测试源码进程已正常退出（exit 0），示例项目未保存修改。重新启动 `.venv/bin/python -m blockstudio.app artifacts/palette-demo.blockproj`，CUA Python / org.python.python。
2. 检查缺口：水印长文字／溢出提示和逐张解析显示；边框叠加时原照片区域传递准确性；流程集中预检数量、缺字段／资源、旧流程兼容；比例约束是否与文档描述一致。
3. 大图与新效果性能（24/约50MP、42素材、10批处理），精细增强取样取消／交互响应。原 0.3.1 的性能报告不能冒充本轮结果。
4. 构建 PostStudio.app 安装 ~/Applications，保留旧应用。签名、实际包操作、保存／重开／退出；A01～A12 验收。
5. README、NEXT_ACTION_PLAN 阶段状态、IMPLEMENTATION、DEVELOPMENT、完整用户手册及离线 HTML 同步，替换旧橙色截图；记录已测／未测第三方往返。当前用户手册仍是 0.3.1，不得声称新版文档已交付。

## 技术注意

- 不能通过更改输入指向已有采样结果来实现重编辑；必须保留原步骤输入。产物独立，旧步骤无同步更新。
- History 不撤销磁盘导出；永久清理重置历史避免指向已删文件。资源当前保守保留，不清理仍由预设／流程引用的水印文件。
- 项目 schema 3，兼容读取 1/2，未知参数／版本要拒绝。纯数据参数、资源只允许 assets 受管路径。
- 新增边框／水印等默认版本已入 tools 注册；项目中 runtime _fields/_resources 不写入持久参数，仅保存解析结果和资源 ID。
- 目录不是 Git 仓库。不得假设有 Git 回滚。

## 2026-09-20 接续增量（优先于上文旧状态）

- 额度恢复后已继续，最近查询五小时 0%、周16%，允许普通使用；保留20分钟自动化。
- 完整 **52 项测试通过，exit 0**。新增重复边框保留原照片区域、增强取样被新预览请求取消。边框内遮时照片参考取仍可见交集，外扩叠加继续传递原照片区域；配色组合传递已有照片区域。
- 水印预览显示当前解析文字和缺字段；超出画面有裁切提示。单字超过行宽时不再在文字临时图中静默裁字；图片水印限制临时像素规模。
- `scripts/benchmark_effects.py` 已运行，`artifacts/effects-v04.json` 是启用渐变＋阴影的小图／24MP／48MP CPU 测试：48MP 交互中位90ms，完整渲染6.822s，峰值3230MiB（不是鼠标到显示延迟）。随后补了取消让位和减少一次无用全图复制，报告是此优化前数据。
- `scripts/validate_v04.py` 是本轮原生45.9MP/42素材/增强效果延迟验收脚本；当前执行中。首次增强效果精细预览等待超时，正在增加诊断、排查，**不可声称新效果原生性能已通过**。最新日志 artifacts/validation-v04.log；测试异常时现会先停止并等待线程再退出，避免误把脚本销毁线程当产品崩溃。
- 已构建并安装 `~/Applications/PostStudio.app`，本地 ad-hoc 签名严格校验通过。**安装包尚不含最后的增强取样取消补丁，最终必须重建替换。** 旧 Block Studio 安装包保留。
- workspace 的 Finder 元信息会导致直接签名 dist 失败：复制到 ~/Applications 时用 shutil.copyfile 保留权限、保留符号链接，不复制文件元信息，然后 xattr -cr 并 codesign --force --deep --sign -，校验成功。不能绕过系统安全弹窗。
- CUA 实际安装包：旧项目打开、流程窗口显示/节点复制、水印窄侧栏无横向溢出、中文签名渲染、拖动水印、⌘Z一次恢复拖动、45.9MP生成水印、另存新项目均成功。保存示例 artifacts/poststudio-v04-native.blockproj。安装包已经正常退出。
- 流程拖入/双击坐标操作被 CUA 的 AXError.notImplemented 阻断，未确认实际拖动；按钮复制成功。仍需测试拖入与排序。
- USER_GUIDE.md 已扩展为19章 0.4候选说明：删除恢复、直接预设、节点流程、资料、边框、水印、精细留白/渐变/阴影；生成离线HTML。旧截图尚未替换，正文仍需最终一致性核查。README和DEVELOPMENT顶部已标候选，NEXT_ACTION_PLAN链接已更新，不要当最终交付。
- 最终交付仍需：排查原生精细预览超时；完成新版性能与42素材验证、拖入排序验证；替换手册截图、统一README/实施记录/计划状态；重新构建签名安装最新源码并重开项目验证；全部完成后停止自动化。


## 2026-09-20 本轮接续结果（最新）

- 最近额度五小时90%、周30%，仍可用。每20分钟自动化保持ACTIVE，尚未最终交付。
- **56 项完整测试通过，exit 0**。新增低分辨率效果缓存坐标、流程预检无副作用、外部拖入事件和内部 MIME 移动。最新细小UI间距改动不影响渲染。
- 原生脚本超时是 QTest.qWait 紧循环影响后台 Python 线程，改为 processEvents＋短 time.sleep 已解决，不是产品精细预览死锁。
- 新增560px效果缓存和渐变背景复用，原生增强取样 P95 从211.64→104.56→**65.68ms**；416输入/392图像绘制，几何20.16ms；42重复公开照片场景刷新.258s，峰值2640.5MiB。报告 artifacts/v04/release-validation.json。
- 新 effects-v04.json 为优化后报告，48MP完整6.216s、峰值2351.5MiB，快速预览中位13.72ms。不是物理显示延迟。
- workflow.preflight 已接入运行流程：资源检查、逐张缺字段、预计产物/文件数；正式点击开始运行后执行。原生列表拖动仍被CUA AX接口阻断，Qt事件及MIME模型路径已测，按钮排序已测。
- 取消取色按钮只在取色时显示，单独一行，避免挤压预览下方操作；为勾选项补最小宽度和间距。
- 回收站清理现显示预计释放空间、可释放与受保护名单，默认取消；资源路径加入清理保护。
- **最新源码已重新构建 dist/PostStudio.app，正在复制安装并签名。** 旧候选保存在 ~/Applications/PostStudio-0.4-candidate-backup.app；Block Studio 0.3.1保留。需确认安装命令结果并实际启动最新版。
- 新增 scripts/capture_manual.py，以公开素材生成 docs/images 下7张新版截图，已查看取样、水印、流程。最后复选框间距微调后可再截图；不需要重复性能测试。
- README、开发文档、手册已同步主要功能和56测试；新增 docs/ACCEPTANCE_0_4.md，明确已测/未测和最小留白语义。HTML生成脚本的版本标签已改；**尚需重新生成HTML、浏览器渲染验证**。IMPLEMENTATION 顶部和NEXT_ACTION_PLAN阶段状态尚待最终统一。

### 剩余交付工作

1. 确认最新安装复制/签名命令结束；通过CUA打开 ~/Applications/PostStudio.app，重开 artifacts/poststudio-v04-native.blockproj，核对新包操作/关闭。若Cua仍无法拖动自定义节点，明确保留手动拖动试用边界，不把MIME测试说成真实鼠标已验。
2. 重新生成截图（最后复选框间距调整）和 scripts/build_user_manual.py；在浏览器实际打开离线HTML核对19章、目录跳转与新截图。
3. 汇总NEXT_ACTION_PLAN阶段、IMPLEMENTATION顶部、DEVELOPMENT顶部、README进度和本接续；链接检查。ACCEPTANCE_0_4.md已提供表格基础，不重复56测试除非有新代码风险。
4. 最终交付新app与完整手册，说明未公证、第三方软件往返未测；全部交付后暂停block-studio自动化。


## 2026-09-20 · 画布右侧工具栏

按用户新反馈移除顶部工具下拉框与“进入工具”按钮；画布右侧独立列出全部五个工具按钮，选中图像后单击进入对应二级页面。工具入口由注册表生成，保留固定输入与重编辑语义。7 项现有 UI 回归通过；另外逐个点击五个按钮核对工具 ID 和所选输入。已更新用户手册、当前界面截图及离线 HTML，并重新构建安装。自动化继续暂停。
